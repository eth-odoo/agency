<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- CRM Lead Form View Inherit -->
    <record id="view_crm_lead_form_agency" model="ir.ui.view">
        <field name="name">crm.lead.form.agency</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_registration" type="object"
                    class="oe_stat_button" icon="fa-file-text"
                    invisible="not registration_id">
                    <span>Registration</span>
                </button>
            </xpath>
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="registration_id" invisible="1"/>
            </xpath>
        </field>
    </record>
</odoo>
